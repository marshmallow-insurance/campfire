name: Preview Bump and Publish

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preview-bump-and-publish:
    uses: ./libs/workflows/semantic-release-preview.yml
    secrets: inherit
